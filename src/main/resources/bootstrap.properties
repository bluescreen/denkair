# Spring Cloud Config bootstrap — wired up in 2018, the config server was never deployed.
# Kept so the deployment script has something to point at.
spring.cloud.config.enabled=false
spring.cloud.config.uri=http://config.denkair.internal:8888
spring.cloud.config.fail-fast=false
